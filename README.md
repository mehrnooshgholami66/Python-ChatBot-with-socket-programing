# 🗨️ Python Group Chat (Socket Programming)

این پروژه یک **چت گروهی ساده** با زبان **Python** است که با استفاده از
**Socket Programming** و **Multi-threading** پیاده‌سازی شده است.

در این برنامه، چندین کلاینت می‌توانند به یک سرور متصل شوند و هر پیامی که
توسط یک کلاینت ارسال می‌شود، به صورت همزمان برای تمام اعضای گروه نمایش داده می‌شود.

---

## 🎯 هدف پروژه
این پروژه به منظور آشنایی با مفاهیم زیر طراحی شده است:
- برنامه‌نویسی شبکه (Client / Server)
- پروتکل TCP
- Socket در پایتون
- پردازش همزمان (Threading)
- ارتباط چند کاربره (Group Chat)

---

## ⚙️ تکنولوژی‌های استفاده‌شده
- Python 3
- socket
- threading

> هیچ کتابخانه خارجی نیاز ندارد.

---

## 🧠 نحوه عملکرد
- یک سرور مرکزی اجرا می‌شود
- هر کلاینت پس از اتصال، پیام‌های خود را ارسال می‌کند
- سرور پیام هر کلاینت را دریافت کرده و به تمام کلاینت‌های دیگر ارسال می‌کند (Broadcast)

---

## ▶ نحوه اجرا

### 1️⃣ اجرای سرور
ابتدا فایل سرور را اجرا کنید:
```bash
python server.py
```
سپس فایل کلاینت را به هر تعداد در ترمینال جداگانه اجرا کنید
```bash
python client.py
```




